@model ContatoModel
@{
    ViewData["Title"] = "Confirmar apagar contato";
}

<div class="text-center">
    <h3 class="display-6">Realmente deseja apagar esse contato ( @Model.Nome )?</h3>    
</div>

<div class="d-grid gap-2">
    <a class="btn btn-success d-block" role="button"asp-controller="Contato" asp-action="Index">Cancelar exclusão</a>
    <a class="btn btn-danger d-block" role="button" asp-controller="Contato" asp-action="Delete" asp-route-id="@Model.Id">Confirmar exclusão</a>
</div>